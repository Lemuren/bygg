# Testing `bygg`

Since `bygg` is a build system it is tested by compiling projects
and asserting that it builds and the resulting binary produces the
correct output.

This directory contains a number of projects and a main test runner script.
